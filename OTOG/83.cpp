#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
template <typename T = int>
T input() { return *istream_iterator<T>(cin); }
struct tree_node
{
    int now;
    int cl = -1;
    int cr = -1;
};
int n;
tree_node BST[100005];
int main()
{
    ios::sync_with_stdio(false);
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        string t;
        cin >> t;
    }
    return 0;
}