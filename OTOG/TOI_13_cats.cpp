#include <bits/stdc++.h>
#define endl "\n"
using namespace std;
template <typename T = int>
T input() { return *istream_iterator<T>(cin); }

int n, q, sum;

vector<long long> v;

int main()
{
    n = input();
    for(int i = 0; i < n; i ++) v.push_back(input());
    
    return 0;
}